# --- UI ---
streamlit>=1.37,<2.0

# --- Orchestration / RAG ---
langchain>=0.2,<0.3
langchain-community>=0.2,<0.3
langchain-text-splitters>=0.2,<0.3
langchain-groq>=0.1,<0.2          # default LLM provider — free tier, no card required
langchain-openai>=0.1,<0.2        # optional — only needed if config.llm_provider = "openai"
langchain-google-genai>=1.0,<2.0  # optional — only needed if config.llm_provider = "gemini"

# --- Vector store ---
faiss-cpu>=1.8,<2.0

# --- Data validation / settings ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0     # BaseSettings moved here in Pydantic v2
email-validator>=2.1,<3.0       # required for pydantic's EmailStr (used in RequirementState)

# --- Embeddings ---
langchain-huggingface==0.0.3 # default embedding provider — runs locally, free, no API key
sentence-transformers>=3.0,<4.0

# --- Data handling / summary export ---
pandas>=2.2,<3.0

# --- Web scraping (to build the RAG knowledge base from virtualemployee.com) ---
beautifulsoup4>=4.12,<5.0
requests>=2.32,<3.0

# --- Environment management ---
python-dotenv>=1.0,<2.0
